const React = require('react')
const { connect } = require('react-redux')

const Landing = () => {
  return (
    <div>
    </div>
  )
}

const mapProps = state => {
  return {

  }
}

module.exports = connect(mapProps)(Landing)
